{
  "slug": "drupal",
  "name": "Drupal",
  "description": "Flexible open-source CMS for content-heavy websites, communities, and custom web applications.",
  "category": "Web Apps",
  "image": "drupal:latest",
  "logo": "https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/png/drupal.png",
  "website": "https://www.drupal.org",
  "docsUrl": "https://hub.docker.com/_/drupal",
  "ports": [
    {
      "host": 8089,
      "container": 80,
      "protocol": "tcp"
    }
  ],
  "volumes": [],
  "envVars": [
    {
      "key": "DRUPAL_DATA_PATH",
      "default": "/opt/appdata/drupal/html",
      "description": "Host path for Drupal files"
    },
    {
      "key": "DRUPAL_DB_PATH",
      "default": "/opt/appdata/drupal/db",
      "description": "Host path for MariaDB data"
    },
    {
      "key": "DRUPAL_DB_NAME",
      "default": "drupal",
      "description": "Database name"
    },
    {
      "key": "DRUPAL_DB_USER",
      "default": "drupal",
      "description": "Database user"
    },
    {
      "key": "DRUPAL_DB_PASSWORD",
      "default": "change-me",
      "description": "Database password",
      "secret": true
    },
    {
      "key": "DRUPAL_DB_ROOT_PASSWORD",
      "default": "change-root-password",
      "description": "MariaDB root password",
      "secret": true
    }
  ],
  "composeOverride": "# Generated by McHarbor App Store\nservices:\n  {{serviceName}}:\n    image: {{image}}\n    container_name: {{containerName}}\n    restart: unless-stopped\n    depends_on:\n      - {{serviceName}}-db\n{{ports}}\n    volumes:\n      - {{env.DRUPAL_DATA_PATH}}:/var/www/html\n  {{serviceName}}-db:\n    image: mariadb:11\n    container_name: {{containerName}}-db\n    restart: unless-stopped\n    volumes:\n      - {{env.DRUPAL_DB_PATH}}:/var/lib/mysql\n    environment:\n      MARIADB_DATABASE: {{env.DRUPAL_DB_NAME}}\n      MARIADB_USER: {{env.DRUPAL_DB_USER}}\n      MARIADB_PASSWORD: {{env.DRUPAL_DB_PASSWORD}}\n      MARIADB_ROOT_PASSWORD: {{env.DRUPAL_DB_ROOT_PASSWORD}}\n",
  "minMemory": "512MB",
  "version": "latest"
}
